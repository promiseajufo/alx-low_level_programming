#include "main.h"
#include <stdio.h>

/**
 *  * mul - muliplies tqow integers
 *   * @a: the first integer
 *    * @b: The seconf integer
 *     * Return: 0 always
 *      */

int mul(int a, int b)
{
	        int result = a * b;

		        return (result);
}
